export const config = ({ config }) => config
export const isLoading = ({ isLoading }) => isLoading
export const loadingMessage = ({ loadingMessage }) => loadingMessage
export const showSidebar = ({ showSidebar }) => showSidebar
export const notification = ({ notification }) => notification
export const pageTitle = ({ pageTitle }) => pageTitle
